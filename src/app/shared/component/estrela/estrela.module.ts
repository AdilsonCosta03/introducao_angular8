import { NgModule } from '@angular/core';
import { EstrelaComponent } from './estrela.componet';

@NgModule({
  declarations: [EstrelaComponent],
  exports: [EstrelaComponent],
})
export class EstrelaModule {}
